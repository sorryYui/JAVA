1.一个类只能有一个父类，但是可以实现多个接口。
2.接口可以有多个父接口，即一个子接口可以对扩展多个父接口。
3.子接口会继承所有父接口所有的常量，如果子接口有父接口相同的常量，子接口会覆盖父接口的常量。
4.子接口会继承所有父接口的方法，如果子接口含有与父接口相同的方法，则子接口会覆盖父接口的方法。如果几个方法的方法名相同，变量数量和变量类型相同，
  但返回值不同，则实现这个接口的类只能实现一个方法，并且这个方法的返回值为所有同签名方法返回类型的子类型。
5.如果父接口定义的方法具有相同的名称，但是参数不同，则实现这些接口的类型以重载的方式实现这些方法。
